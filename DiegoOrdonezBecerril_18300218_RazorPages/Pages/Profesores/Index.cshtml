@page
@model DiegoOrdonezBecerril_18300218_RazorPages.Pages.Profesores.IndexModel
@{
    ViewData["Title"] = "Profesores";
}

<div class="container row">
    <div class="col-6">
        <h2 class="text-info">Listado de profesores</h2>
    </div>
    <div class="col-6 text-right">
        <a asp-page="Create" class="btn btn-primary">Agregar un nuevo profesor</a>
    </div>
</div>
<form method="post">
    @if (Model.ProfesoresF.Count() > 0)
    {
        <br />
        <table class="table table-striped border">
            <thead>
                <tr class="table-secondary">
                    <th>@Html.DisplayNameFor(model => model.ProfesoresF.FirstOrDefault().Nombre)</th>
                    <th>@Html.DisplayNameFor(model => model.ProfesoresF.FirstOrDefault().FechaNacimiento)</th>
                    <th>@Html.DisplayNameFor(model => model.ProfesoresF.FirstOrDefault().Sexo)</th>
                    <th>Editar | Eliminar</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.ProfesoresF)
                {
                    <tr>
                        <td>@Html.DisplayFor(model => item.Nombre)</td>
                        <td>@Html.DisplayFor(model => item.FechaNacimiento)</td>
                        <td>@Html.DisplayFor(model => item.Sexo)</td>
                        <td>
                            <button asp-route-key="@item.Key" asp-page-handler="Delete" onclick="return confirm('¿Estas seguro de que deseas eliminar el registro?')" class="btn btn-danger btn-sm">Borrar</button>
                            <a asp-route-key="@item.Key" asp-page="Edit" class="btn btn-success btn-sm">Editar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <label>Total de profesores: @Model.ProfesoresF.Count()</label>
    }
    else
    {
        <div class="alert alert-info">Aún no hay ningún profesor</div>
    }
</form>